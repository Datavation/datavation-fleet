# context/projects.md — Cody

Active builds, status, blockers. I keep this current as I work. Holly and Austen
prioritise; I execute.

-----

## Status (2026-07-02)

| Project | Status | Next | Blocker |
|---|---|---|---|
| **Governance tree + hierarchy validator** (commission `Shared\Reports\Commission-Cody-Governance-Tree-and-Hierarchy-View-2026-07-22.md`; standard `Tabularium\Canon\Architecture\Agent-Delivery-and-Governance-Model.md` ratified 22 Jul) | **PHASE 0 PLAN DELIVERED 2026-07-22, HARD STOP — awaiting Rex's 16:30 review.** Plan: `Shared\Reports\Phase-0-Plan-Governance-Tree-and-Hierarchy-View-2026-07-22.md`; card `3a506d59-4638-815e-a970-c4ad9466912c` (Feature under Governance Epic → Agent Fleet Pillar, legal edge, 🔴 Rex). **Nothing built, no Notion write except the plan card itself.** Three blockers raised rather than worked around: **(A)** new DB has nowhere to live — Decision Board is at workspace top level and the API cannot create a DB there; Rex must create + share a parent page with Rex Command Bot. **(B)** "enforce in the schema" is unbuildable — Notion has no conditional validation; proposed chokepoint enforcement (write path refuses + Defect formula + validator blocks closure) needs approval as a conscious substitute. **(C)** "Amends the estate" has no Notion object to point at — Canon Change Log holds *proposals* not artifacts; recommend a Canon file path validated against the real Tabularium. **Measured vs commission:** board 265 not 284; the 20 message cards ALREADY archived (migration step 6 done); docket children 77 not 76 (24 agenda items ✓, 53 decisions not 52); illegal edges 46 ✓ exactly. **The duplicate Rex was to rule on no longer disagrees** — all four Vercel copies now read Done. Derived docket state to be computed in Python, not Notion rollups (3 hops, last cross-DB); Docket gets **no status property at all**. Migration is copy-never-move → rollback = drop the new DB, board untouched | Rex approves plan + does (A), rules (B)/(C) → then Build 1 schema, Build 2 migrate one docket & stop for review, Build 3 validator + the two defects (Kanban "docket"→"Epic" filter, Docket "(done)"→derived three-state) | **A/B/C ALL CLEARED + BUILD 1 & 2 DONE 2026-07-22.** (A) Rex created the Governance table in the UI (workspace-level, API can't) + shared it with Rex Command Bot — reachable. (B) signed off — chokepoint enforcement, DMS revisit later. (C) I checked Canon and CORRECTED my own recommendation: link target is the version-less **document name** not a path (`Agent-Library-Filing-Standard` §4 forbids versioned cross-references; six live standards would break on the scheduled rename pass). **Build 1:** schema live (22 props, 4 tiers, Disposition, one-directional link to board so no column added to it, Docket has NO status property), Technical Review Board stood up (§11), Defect formula proven to fire/stay-quiet. Idempotent. **Build 2:** all 4 dockets migrated — 82 rows (1 Forum, 4 Dockets, 24 Agenda items, 53 Decisions), copy-not-move, board schema fingerprint `bf42e4ce0966` identical before/after, every verify PASS. 53 decisions carry NO disposition on purpose (not guessed — §9.4 human backfill). **Structure explainer built:** `Shared\Boards\Governance\Governance-Structure.html` — nesting tree + disposition bridge + ERD (self-joins + cross-DB joins), browser-verified responsive. Scripts: `_govstore.py` (schema+forum), `_migrate.py` (list/one/verify). **Findings raised, not mine:** a 5th docket now exists (`trb-2026-07-22`, TRB first sitting) on the board+Docket.html but NOT migrated; six live Canon standards still versioned (§4); 7 standards lack frontmatter `id` (§5); `_catalogue.md` missing (§6.1, Libby's). **Startup gap: my CLAUDE.md does NOT load `Engineering-Design-and-Build-Standards.md`** (which holds working-software-over-docs) — flagged to Rex, needs his yes to add (identity gate). **BUILD 3 DONE 2026-07-22 (while Archy rules on the escalation):** (a) **Kanban defect** — the "docket" filter (grouped by top-level Epic, read as 16 dockets after the backfill) renamed to **Epic** (`Which Epic`/`All Epics`), `dockets()`→`epics()`. (b) **Docket defect** — the picker's misleading "(done)" (meant "all answered") replaced with derived three-state; **Estate Audit now reads "ruled · 12 in flight"** not done, browser-proven. (c) **Hierarchy validator** — extended Archy's mockup `Shared\Boards\Hierarchy\_hierarchy.py` into the real §7 compliance surface: now reads **both stores**, reports all four violation classes (illegal edges live-first, orphans, ruled-never-commissioned decisions, dockets in Ruled state), governance section links to Notion, docket-Epic note updated (model resolved it — they're migrated, pending board retirement = Rex's hand), title neutralised pending Archy's naming ruling. Wired into the **sweep** (rebuilds hierarchy too) and the **watermark now covers both stores** (a disposition edit in the governance store triggers a rebuild — [[board-views-must-follow-the-store]]). Fleet-Home already links it. All builds green, 8+1 script blocks `node --check`, responsive + dark-mode-aware, snapshot taken. §9.4 disposition backfill still pending (needs Rex/rule). **ARCHY RULINGS EXECUTED (partial) 2026-07-23** (`Shared\Reports\Archy-Rulings-Governance-Structure-2026-07-23.md`; card `3a506d59-4638-815e-a970-c4ad9466912c` → 🔴 Rex): board ratified as **"Delivery Board"**. DONE+PROVEN: (1) re-brand delivery surfaces (Kanban+Hierarchy chrome → Delivery Board, naming-note cleared; residual strings are card DATA, left alone); (3) 5th docket "TRB first sitting" migrated (11 rows, TRB now 5 dockets, verify PASS); (4a) **reconciliation manifest** `Shared\Reports\Reconciliation-Manifest-Docket-Retirement-2026-07-23.md` via `Governance\_manifest.py` — RETIRE 87 / RE-HOME 5 (Estate Audit ADOPT cards, reassign never delete) / KEEP 0. HELD for Phase-0 go (`Shared\Reports\Phase-0-Docket-Surface-Repoint-2026-07-23.md`): item 2 re-point (publish+build+apply of the Docket surface board→governance, big-bang, 3 coupled halves) + item 5 dual-nature state.py (§4.1 both-links closure). Two forks for Rex: (A) does "re-point the Kanban" mean it reads governance or just stays a delivery board that sheds retired cards [recommend latter]; (B) answering a governance decision = record the Ruling only (disposition is TRB-ratified, not a click). NOT mine: retirement (Rex's hand off manifest), RE-HOME targets (Archy), 53→57 dispositions (Archy proposes→TRB ratifies→my write path records). **RUN COMPLETE 2026-07-23 — all 5 items done+proven.** Rex ruled both forks: **A** = re-point the Kanban too (it now reads BOTH stores: 158 delivery cards + 57 governance decisions in ⚖-marked docket lanes, governance columns from derived state); **B** = a governance decision delivers a *rule*, and a click marks it complete → wired as `Rule delivered`+`Delivered on`, set by drag-to-Done or a completion verdict, closing the RULE half only, never touching a linked delivery card and never inventing a Disposition. **Re-point:** `_docket.py` publish/read + `_docket_apply.py` all moved to governance together; `state.json` holds governance ids via new `restate-all`. **Proven end-to-end** on a throwaway docket: published → 3 rows in governance, **ZERO leak to the Delivery Board**, fingerprint `bf42e4ce0966` unchanged → ruling landed → Disposition stayed empty → throwaway archived. **`state.py` + 28 offline tests ALL PASS** incl. Archy's §4.1 gap (both links checked regardless of the single Disposition; unknown card status never rounded up). **Bug caught+fixed:** hierarchy rebuild failed under the sweep but passed by hand — sweep captures stdout as cp1252 and Pillar titles carry emoji → UnicodeEncodeError; sweep correctly refused to advance its watermark; all 3 generators now own their stdout encoding; sweep green on all 4 pages. Review copies: `Agents\Cody\builds\{Docket,Kanban}-v2026-07-23-governance-repoint.html`. Still not mine: 46 illegal edges (Archy), status collapse 14→6 (last), Strategy/Data-Governance forums (§3.2, gated on dispatcher `3a`) |
| **Docket & Kanban surfaces** (handed over from Archy 2026-07-22, `Shared\Reports\Handover-Docket-and-Kanban-Build-for-Cody-2026-07-22.md`; tracked on Operating Layer docket items 2b + 5a — **no parallel list**) | **ARCHY'S TWO LIVE BUGS FIXED + VERIFIED 2026-07-22** (card `3a506d59-4638-8198-9826-ff42aed92eba` → To=Archy/Review; report `Shared\Reports\Completion-Report-Docket-Sync-Bugs-2026-07-22.md`). Both root-caused to `_sweep.py`, neither where the write-up placed them. **Bug 2 (chat never delivered):** `pending()` never globbed `rex-question-*` — the applier always handled them, nothing ever handed them over; Rex's 3 messages sat on disk, spinner spinning. Delivered (card 2c on *Rex's Hands*, not Operating Layer's 2c — Archy checked the wrong card). **Bug 1 (stale views):** the sweep rebuilt ONLY after applying a dropped file, so every direct Notion edit — how all agents write — never regenerated anything; 11 quiet passes, no rebuild. Now follows the board via a cheap `last_edited_time` watermark probe, throttled, advancing only on a clean full rebuild (fail path proven live). Plus `updated N min ago` + Refresh + guarded refresh-on-return, since fixing the file doesn't help an open tab. **Proven on Archy's own change: 2a/2b render Done in the Kanban DOM; scheduled sweep fired unattended at 11:46.** Also fixed: my own refresh guard (stale closed-dialog text blocked refresh forever) and thread attribution labelling every non-Rex agent as Archy. **Separate finding, staged not deployed:** `sync_site.py` globbed `Boards\*.html` (top level) so the real surfaces were NEVER hosted — the deployed site links the *archived* Weekend Kanban. Glob fixed + guards added; no sync, no deploy (gated on item 3). — **BACKLOG ITEM 1 (merge the dialog) DONE + VERIFIED 2026-07-22.** New `Shared\Boards\_dialog.py` holds the decision UI once — options, chat-bubble thread, note box, and the ONE verdict→queue map — spliced into both pages at build time, so each generated HTML stays a single self-contained file (invariant 6 held). Rex ruled three calls: shared renderer in two containers (Docket keeps its inline panel, Kanban its popup — Docket's batch-answer flow untouched), Kanban's bubble thread wins, unlock-to-revise now applies on both. **Caught 4 live routing bugs**: `Un-park both` routed to `later` so the applier PARKED what Rex had just un-parked (the named "keyword routing is fragile" trap, still live on the verdict); `Keep parked` applied as AGREEMENT on the Kanban; note-only applied as agreement there too; `problem` unrouted on the Docket. Also: duplicate "Need more info" button gone; `_sweep.py` now globs `rex-question-*` (it never did — a question sent alone sat unapplied). `_docket_apply.py` **unmodified**, contract proven by dry-run against the page's real payload. Verified: 8/8 script blocks `node --check`, 29-verdict parity table, both builds exit 0 (4 dockets / 170 cards), both surfaces driven live in a browser. Report `Shared\Reports\Completion-Report-Merge-the-Dialog-2026-07-22.md` | **Backlog items 2–6** in handover order: host it (unlocks iPad nav + folder picker + ElevenLabs voice) → gate `/api/propose` → fold into Fleet Command → Engine Room view → card detail without the dialog. **Rex:** screenshots-on-Docket needs an applier change (literal `kanban-question` match) — raised, not built | None. Rex's `Level`/`Parent` backfill lands in parallel and only makes the Kanban's docket filter and swimlanes more useful |
| **Cody Verification Loop v0.1** (Rex build-spec 2026-07-18, Fable session; TDS `Oz Files\Design\Cody-Verification-Loop-TDS-v0_1.md`) | **BUILT + LIVE-FIRED 2026-07-18** — `routines\cody-session-end-verify.md` (first routine in my seat, to Routine-Standard v0.1); §3.3 honest-completion contract verbatim + §3.4 Marshall-freedom clause in my CLAUDE.md (Archy-gate line reconciled); DoD standard authored (rubric format + worked example) **staged** `Builds\definition-of-done-standard-v0_1\` — spec's premise it was scaffolded in Canon was FALSE on disk, and my Tabularium deny-gate (correctly) blocks the write. Routine fired live against this build itself: **NOT DONE — 6/7 PASS, criterion 5 FAIL (Canon file), escalated.** Report `output\specs\2026-07-18-verification-loop-completion-report.md` | **Rex: copy staged DoD standard → `Tabularium\Canon\Architecture\`** — then criterion 5 flips PASS and the item closes DONE | Rex promotion — control gate (the gate holding under a spec that asked to cross it is the gate working) |
| **Fleet Command — implement Rex's design (Lane 1/B3 REISSUED)** (Archy commission 2026-07-17 06:35, card `3a006d59-4638-8128-b66b-ffc30ec4f822`; ruling: **Rex designs / Cody implements / Archy reviews**) | **BUILT + SELF-TESTED 2026-07-17** — `design\Fleet-Command.dc.html` ported 1:1 to vanilla-JS SPA: `static\js\fleet\` (10 modules) + `css\fleet-command.css` + rewritten `index.html`. All 8 areas + agent detail (10 profile sections), ops kanban DnD, dispatcher toggle. Stub data in `data.js` shaped exactly like `/api/board` payload (swap = replace `loadBoard()` body). Scaffolding stripped, fonts kept. pytest **79/79**; 10 screenshots `docs\fleet-command-v1\`. Ambiguities logged on card (design's 5-seat fictional roster vs real 11; `var(--x)22` badge quirk preserved; hash deep-links added for self-test; old front-end files dead-in-place — remove at promotion). **Recovery finding:** pre-06:05 1,109-line app.js NOT recoverable locally (no git, no copies); OneDrive web version history = Rex's hand. Handoff `Builds\pc-cockpit\BUILD-HANDOFF-fleet-command.md` | **Archy: gate-review** (report_back fired) → Rex 18:00 pass corrects assumptions; plumbing phase gated on Rex UX sign-off | Archy gate — control gate. Real data wiring explicitly gated on Rex's UX sign-off |
| **Notion Data Export Pipeline** (Rex brief v0.1 2026-07-15, Archy-ratified; board card `39e06d59-4638-8117-bf69-ca05b6ab2645`) | **PHASE 1 BUILT + 15/15 OFFLINE SELF-TEST GREEN 2026-07-16** — `Builds\Apps\notion-data-export\v1\`. Read-only extract: REX Addresses/Jobs/Tasks/Photos → `Rex-Ops-Data-Export.xlsx` (one sheet/table, frozen header, Arial), `contains_pii`/`contains_sensitive_note` flags, ratified first-link+`address_link_count` multi-address handling. Control gate enforced IN CODE (`ENDPOINT_ALLOWLIST` + `_request()` — only GET `/databases/{id}` and POST `/databases/{id}/query` pass; verified a same-path PATCH is refused). No live Notion call made — blocked on credential. Handoff `Builds\Apps\notion-data-export\v1\BUILD-HANDOFF-PHASE1.md` | **Rex: provision new read-only Notion integration**, share with the 4 data sources, drop token into `C:\AgentEngine\secrets\notion-data-export.env` (template `.env.example` in build folder) → then Cody runs the first live pull | Rex credential provisioning — control gate. First live run (schema confirm + real row counts) unprovable until token exists |
| **Cockpit Agent Chat Pane** (Archy spec 2026-07-15, URGENT; Fable) | **BUILT + SELF-TESTED LIVE 2026-07-15** — click agent → chat pane → agent wakes on own seat → replies in pane → per-seat persistence. Reused: SeatSessions `--resume` (S3), runner wake path (cockpit writes `chat_queue\<Seat>\` marker, NEVER spawns), SSE stream (now tails dispatcher + `chat_wakes` union), session-reset (+transcript archive). New: `POST/GET /api/agent/{name}/chat`, chat UI (`#chat/<Seat>` hash routing), transcript `state\chat\<Seat>.jsonl`. **FLAGGED runner touch:** `local_runner.py` chat-queue consumption on 2s local tick (board poll unchanged 10s), stream-json wake logs, all rails applied (identity gate/seat_lock/caps/bounded-retry/fail-loud VOICED in pane, log-sealing vs ghost-running). Tests 79+77 green. Live proof: Holly hello→reply 29s, memory check passed (--resume), refresh persisted, Libby isolated, reset→fresh session proven, Quill (unborn seat) refused LOUDLY in pane. Screenshots `pc-cockpit\docs\chat-pane-*.png`. Both scheduled tasks restarted onto new code. Handoff `output\specs\2026-07-15-cockpit-chat-pane-handoff.md`; Tessa gate card `39e06d59-4638-81fc-aea8-fd9ae65078d2` | **Tessa: live functional+visual gate + screenshots** → Archy eyes on the runner touch → Rex acceptance (refresh cockpit, click agent, chat) | Tessa's wake path = the stale dispatcher (Archy chasing separately — chat itself does NOT depend on it) |
| **Fleet Console — Sessions 2+3 (pc-cockpit extension)** (Archy commissions 2026-07-14 to the RATIFIED build plan; manual launches, Rex's in-session "Go" for S3) | **SESSION 3 ALSO BUILT + STAGED 2026-07-14** (same session) — A) per-seat serialization exactly to Archy's C1–C4 (seat-tagged shared `_active` spanning dispatch+review passes; pre-claim skip-and-continue; queue-never-drop via unclaimed re-poll; seat lock = view over pruned set, crash frees seat) + cap 1→2 (3 deferred; 6/hr flagged untouched), dispatcher selftest **18/18** = the §5.3 verify line offline; live 2-real-wake isolation check → Rex activation checklist. B) card chat: runner `SeatSessions` one-per-SEAT (`--session-id` first / `--resume` after; Gate 0.2 ~5× cheaper), cockpit New-conversation → reset MARKER consumed pre-wake (runner stays single writer), session-shaped resume failures auto-discard (never debug), board record survives resets (pinned), memory-discipline line in wake prompt, session ids never leave the server; telegram-cockpit **64/64**, pc-cockpit **61/61**, UI verified in browser. FLAGS to Archy: cross-poller same-seat overlap (dispatcher+runner can wake one seat concurrently — discovered, not built); sessions.json in OneDrive-synced state dir; §5.2 review-date = Rex's card. S3 review card `39d06d59-4638-8196-96a7-e7e82fd4c7f2`; gate covers combined S2+S3 diff. — *Session 2 (earlier same day):* **BUILT + STAGED 2026-07-14** (Fable) — all six §4 Immediate items in ratified order 6→2→3→4→7→5, per-item verified. Item 6: both pollers → 10 s (caps asserted untouched). Item 2: `ops\install-tasks.ps1` (at-logon/logged-on-only/restart×3/IgnoreNew; **refuses to register without the Telegram alert channel** — §5.1 cond 1 at infra layer; ASCII-only for PS 5.1 no-BOM) + crash-safe PID-checked/self-clearing lock + started/restarted/crash-loop alerts in both pollers (§6 live touches, flagged). Item 3: atomic UTC-epoch heartbeats per cycle, `/api/health` per-poller up/stale (3× own interval), sticky green/amber/red "NOTHING IS WATCHING" banner (verified live green→red→green in browser), KILL/rate-cap/HALT transition alerts. Item 4: `COCKPIT_BIND` slot wired (Tailscale NOT yet installed — value is Rex's, post-install); cockpit task delayed after Tailscale. Item 7: Needs-Rex pinned / Running (from wake-log scan, filename-stamp based, `type=result` finalize detection) / rest collapsed; verified on real board desktop+phone, screenshots in `docs\item7-*.png`. Item 5: consumer check done (report_back + review flows read board, never logs) → `spawn_wake` flipped to `stream-json --verbose` (selftest-pinned) + SSE `/api/agent/{name}/stream` (?key auth, torn-line holdback, wake-switch, bounded clients) + live card pane (verified growing mid-run). Tests: pc-cockpit **57/57** (25→57), dispatcher **14/14**, telegram-cockpit **55/55**. Rails: envelope byte-identical, parity green, both caps untouched, no Session-3 work. Ops modules canonical in `pc-cockpit\ops\` + byte-parity-tested copies beside both pollers. Handoff `Builds\pc-cockpit\BUILD-HANDOFF.md`; review card `39d06d59-4638-8141-acd6-e7a0c70f9d7a` (dispatcher already woke Archy — review leg live); 🔴 Rex activation card `39d06d59-4638-816b-a255-d1e4adeafccd` | **Archy: gate-review** (wake already fired). **Rex (activation card):** provision `TELEGRAM_BOT_TOKEN`+`TELEGRAM_ALLOWED_USER_ID` into both poller envs → after gate: stop manual consoles, run `install-tasks.ps1`, acceptance checklist → any time: Tailscale PC+phone + `COCKPIT_BIND` | Archy gate + Rex hands (alert values, task registration, Tailscale) — control gates. ⚠ live dispatcher/cockpit still run OLD code until restarted; old dispatcher holds no PID lock — stop it at first task start |
| **Rex Ops Dashboard → Vercel** (Rex brief 2026-07-13) | **DEMO DEPLOYED LIVE 2026-07-13** (Fable→Opus) — `Builds/rex-ops-dashboard/` → private `Datavation/rex-ops-dashboard` → **https://rex-ops-dashboard-demo.vercel.app** (team `datavationuk`, Hobby; renamed for `-tpg`/`-equans` siblings). Shipped as-is + noindex + robots. All DoD verified. **Rex rulings this session:** Marshall DEVELOPS / Cody only DEPLOYS; TPG+Equans = REAL data → GDPR-strip to addresses/locations/times/types (drop names+street/number); client-side login ≠ security (data ships to browser) → real client data needs Vercel Pro / backend / **authed subdomain of RHS/Datavation** (e.g. equans.datavation.co.uk, branded RHS offering); login-logging built-in but OFF (Sheet or Notion). Handoff `output/specs/2026-07-13-rex-ops-dashboard-deploy-handoff.md` | **Marshall: build** dashboard to Rex's requirements (Cody's constraints folded in by Rex). **Cody: deploy when Rex says ready.** Open decisions parked to next session: log destination, gate style, subdomain mechanics | Session paused 2026-07-13 (Rex changing gears); Marshall's build is the gating dependency |
| **Agent-Dispatcher: N-seat generalization (Tessa leg)** (Archy commission 2026-07-13, board card `39c06d59-4638-81e0-a4fe-c0d4123373d6`; Sonnet 5, Rex-approved) | **ARCHY GATE PASS + TESSA QA-VERIFY PASS, both 2026-07-13** — patched `Builds\agent-dispatcher-v1\` in place (never promoted/started live, no v2 fork). `DISPATCH_TARGET`/`REVIEW_TARGET` → `BUILD_SEATS`/`REVIEW_SEATS` sets; wake target read from the card's own `To`, validated against the table; `board.py` filters generalized to an OR-of-equals; `review_prompt()` picks Tessa's qa-verify wording vs Archy's gate-review from a task-desc table. Self-test 14/14 in-place + clean-copy, same OS. Tessa independently read the code, grepped for promote/credential paths herself, and — notably — fetched the LIVE Notion board schema directly and confirmed "Tessa" + all needed select options genuinely exist, resolving the one honest limit that couldn't be closed offline. Verdict `Agents\Tessa\output\reviews\2026-07-13-agent-dispatcher-v1-qa-verdict.md`, read and confirmed genuine. Card `39c06d59-4638-81e0-a4fe-c0d4123373d6` → Status Done / ⚪ None. Handoff `Builds\agent-dispatcher-v1\BUILD-HANDOFF.md` §7 | **Rex: activate** per handoff §6 (unchanged from the original build — provision wake credential, watch one cycle, start the loop) | Rex activation — control gate. Real `claude -p` wake + live REST round-trip remain genuinely untested (both require Rex's watched first cycle by design) |
| **Quinn (Quaestor) CFO seat v1** (Archy commission 2026-07-11 incl. Rex's §10 locked rulings + §11 plugins; Fable per-task grant) | **BUILT + 17/17 SELF-TEST GREEN 2026-07-13** (Windows PS 5.1 / Py 3.14.4, synthetic figure-free fixtures) — `Builds\quinn-cfo-v1\`. Full seat: CLAUDE.md (CFO board seat, reports to Rex) · 5 routines to Routine Standard (weekly review, month-end close w/ coverage block, 30/60/90 forecast w/ payroll-test, receivables watch DRAFT-only chases, consolidated_view→dashboard) · 4 skills (consolidate = finance-engine SALVAGED, launchers dropped + test-enforced gone, NEW quickbooks_csv extract adapter, NEW asset class/net-worth manual lines; other 3 ADAPT Small Business + Finance plugins per §11) · capability-manifest + client-config seam (figure-free, `<SET-ON-PROMOTION>`) · Memory v0.2 pair empty-at-birth · `quinn-settings.json.PROPOSED` money control gates (all QBO write/send tools wildcarded-denied). **ARCHY GATE PASS + TESSA QA-VERIFY PASS, both 2026-07-13** — Archy independently re-ran self-test (17/17); Tessa independently qa-verified (17/17 in-place + clean-copy, exact deployment OS, own CLI runs beyond the self-test, own tree-wide network grep) — verdict `Agents\Tessa\output\reviews\2026-07-13-quinn-cfo-v1-qa-verdict.md`, read and confirmed genuine. Card `39c06d59-4638-817a-85b6-f6207adfdb56` → Status Done / ⚪ None. Handoff `Builds\quinn-cfo-v1\BUILD-HANDOFF.md` | **Rex: promote** per handoff §6 (workdir init + local accounts.csv fill + first-run tie-out eyeball) | Rex promotion — control gate. QB/PDF adapters confirmed on first real extracts only (flag-don't-lie if layouts differ) |
| **Voice Agent Engine — Phase 2 voice adapter + secrets relocation** (Archy Phase-2 TDS 2026-07-10, ratified) | **LATENCY FIX BUILT 2026-07-10 eve (21/21)** — pilot ran E2E but FAILED the latency kill-condition (3.5s full-reply buffering → ElevenLabs re-fires, 20+ interim logs, no audio). Fixed same day per Archy fix brief: **token-streaming** (`llm.stream_reply` off Anthropic messages.stream; additive `Engine.handle_stream`, `handle()` byte-unchanged; first-piece primed pre-headers so write-first timing identical; R2 correction APPENDED on stream — ruled) + **interim-turn dedup** (exact retry → cached replay, engine untouched, logs once; prefix-grown → supersede marker + in-flight cancellation with `interrupted` write; failures never cache). Measured first chunk ~6ms over real socket. Spec addendum `BUILD-SPEC-PHASE2.md` §8; handoff `output\specs\2026-07-10-voice-latency-fix-handoff.md`. **Rex: RESTART the adapter process to pick it up.** — Earlier same day: **BUILT + 15/15 OFFLINE GREEN** (Fable) — extends `Builds\voice-agent-engine-v1\`, **engine/brain untouched** (Phase-1 suite re-run 21/21 inside the Phase-2 suite). `channels\openai_api\` = OpenAI-compatible `/v1/chat/completions` SSE for ElevenLabs Custom LLM: Bearer `VOICE_SHARED_SECRET` auth (refuses to start unset) → engine path (`channel=voice`, write-first) → sentence-chunk SSE with token-streaming seam. cloudflared quick-tunnel script + named-tunnel doc (`tunnel\`). **Secrets relocation DONE**: 3 synced .env (pc-cockpit, tg-runner, Cody) → `C:\AgentEngine\secrets\`, readers repointed, tools verified post-delete; runner via [PR #5](https://github.com/Datavation/telegram-cockpit/pull/5) (55/55, branch live on PC). Report `Builds\secrets-relocation-2026-07-10\`. Handoff `output\specs\2026-07-10-voice-adapter-phase2-handoff.md` | **Archy: gate-review** → Rex: `VOICE_SHARED_SECRET` in `C:\AgentEngine\.env` · `winget install Cloudflare.cloudflared` · ElevenLabs agent (Custom LLM, tunnel URL/v1, model `holly`) · merge PR #5 · **kill-conditions first** (latency/tools-by-voice/voice) — abort cheaply if unfixable. FLAGS: `Agents\Holly\webhook\.env` = 4th synced live .env (behind my write gate, untouched); rotate relocated tokens | Archy gate + Rex provisioning — control gate. Voice acceptance unprovable until ElevenLabs provisioned |
| **Voice Agent Engine — Phase 1** (Archy TDS v0.1 2026-07-09, ratified; Rex brief = build authority) | **BUILT + 21/21 SELF-TEST GREEN 2026-07-09** (Fable) — `Builds\voice-agent-engine-v1\`. Reusable text-agent engine to the 4-line contract (persona/playbook/data-target/whitelist, loader rejects a 5th) · sacred write-first JSONL loop (`C:\AgentEngine\<agent>\conversations\`, append+fsync before brain, stdlib-only write path) · idempotent Notion sync worker (high-water mark + Sync Key query-before-create; outage/replay proven vs mock) · console + web chat channel (127.0.0.1:8377 — the datavation.ai mechanism) · **Holly RE-BASED one-Holly** from hosted webhook: persona/context mechanically derived + hash-verified, 3 known diffs on the record (`migration\REBASE-REPORT.md`). Handoff `output\specs\2026-07-09-voice-agent-engine-phase1-handoff.md` | **Archy: gate-review** → Rex promotes: `C:\AgentEngine\.env` (API key, Notion token, `NOTION_CONVO_DB`) + add `Sync Key`/`Conversation` props to Convo Log DB → run per README. Then: Quill instance (next session, 4 lines); phase 2 = ElevenLabs voice adapter | Archy gate + Rex credentials/promotion — control gate |
| **Field Reporting v1** (Rex Field Operations, TPG/Equans MVP; Archy commission 2026-07-06, Rex-approved same day) | **BUILT 2026-07-06** (Fable 5, in-window) — `Builds\field-reporting-v1\`. Config seam (client-config.psd1 + loader; ACME swap test = DoD#4 proven), 5 Marshall scripts re-based hardcode-free, vision pass (routine + flag-block writer + structural export guard, exit 2) gated behind `Invoke-VisionTrial`, invoice tail (`New-TPGInvoiceDraft.ps1` real PDF attach by ref, draft-only structural; pre-invoice routine formalised). OAuth creds moved out of code (secrets-file pattern; rotation recommended). Offline tests **38/38**. Handoff `output\specs\2026-07-06-field-reporting-v1-handoff.md` | **Archy: ratify** → Rex promotes (copy to Marshall master + `Marshall Scripts\`, create client-secrets.psd1 by hand) → P-1..P-6 verification on Windows (P-2 = vision dry-run trial is the go/no-go for live step 5b) | Archy ratification + Rex promotion — control gate |
| **Dex knowledge engineering + Libby engine** (Archy commission 2026-07-05, RATIFIED) | **ALL GATES 0–4 BUILT 2026-07-05** (Fable, overnight run under Rex's in-session standing approval). Gate 0: recon 2,341 files/0 errors, skip+discard fully logged, Libby scaffolded. Gate 1: taxonomy (DAMA+DCAM+strategic+career) / 7-authority register / node schema in `engine\config\`. Gate 2: 28-node curated subset (13 knowledge + 8 principles + 7 authorities), all claims source-cited, `[NEEDS:]` gaps; knowledge-map + alias-index GENERATED from front matter. Gate 3: Dex scaffolded (boot-light contract + standing retrieval instruction; library/G: read-only at infra layer); Kimball fidelity test ran unprompted → grounded cited answer, PASS proposed (voice grade = Rex + live re-run at promotion). Gate 4: lookup.py + 24-probe suite → tuned to 24/24 hit / 24/24 false-reach clean. Promotion checklist in build README. Board card updated + verified. Scope gate held: `Builds\` only throughout. **PROMOTED to `Agents\` 2026-07-06; COMPLETENESS bundle staged `Builds\dex-libby-completeness\` 2026-07-07** (engine→Libby live home w/ live-repath, STAGED→LIVE flip both, Dex scaffold to standard, memory→memory.md, Notion `_mcp_note`) — verified test_lookup 24/24 + build_map byte-exact vs live index; card `39506d59…` → In review / 🔴 Rex, To=Cody | **Rex: promote per `Builds\dex-libby-completeness\PROMOTE-LIST.md` → Cody VERIFY-AFTER-COPY → Archy self-consistency check** | Promotion — Rex's hands; then Cody verify, Archy confirm |
| **austenking.com site v1** (Archy commission 2026-07-05; estate site #3, KPI pillar) | **READY TO DEPLOY 2026-07-05** (Fable 5) — built, then Rex ruled **skin A Ink & Ember** + dropped real photos; de-review done (toggle stripped, skin promoted to design-system as `skin-austenking.css`), hero now the REAL cc headshot, 1600px web derivatives generated, re-verified clean. `Builds\austenking-site-v1\` self-contained. Handoff `output\specs\2026-07-05-austenking-site-v1-handoff.md`; board card Approved / 🔴 Rex | **Rex: deploy** (control gate). Post-deploy: apex SSL tidy (spec §9), per-article LinkedIn URLs for Writing cards, spare photos available for extra slots (BUILD-NOTE) | Rex deploy — control gate |
| **Fable web-estate build** (3 sites + Quill/Ivy; Archy commission 2026-07-04) | **.CO.UK v2 (consultancy, 10 pages incl. Chowa Method page) + .AI (8 pages, live client-side Quill diagnostic) BUILT + SELF-TESTED 2026-07-04** per CONSULTANCY-SCOPE-BRIEF-v2; quill scripted-core engine + augment-fill playbook staged canonical in `Builds\fable-web-estate\quill\`. All sweeps PASS both sites; mock figures in one file/site + MOCK-FIGURES.md. Running on the borrowed Mac (Fable) | **Archy: review both sites** (board card) → Rex eyeball on connect-graph demo (`design-system\connect-graph.html`, built 2026-07-05, NOT wired into .ai hero yet) → then Rex site → Quill step 5 → (headroom) Dex method doc. **Mac session closed 2026-07-05 — resume on PC via `Builds\fable-web-estate\PICK-UP-HERE.md`** | Archy review gate. Open inputs: CJ typefaces + logo files, form destinations, calendar link, real Rex proof figures + pricing |
| **Fleet permissions baseline** | **STAGED 2026-07-02** — 5 × settings.json written; Cody self-applied; Archy/Hobbs/Holly/Marshall in `Builds\fleet-permissions-baseline-v1\`; board card In review / 🔴 Rex | Rex: copy 4 files to live seats (BUILD-HANDOFF.md has exact cmds; Holly + Marshall need `.claude\` dir created first) | Control gate — Rex hand only |
| **Dispatcher v2 / always-on Archy (Phase 0)** | **PHASE 0 FILED 2026-07-02** — architecture plan + Rex prerequisites written to board card `39006d59-4638-818f-8be7-f2fa4c991729`; card → Archy for review | Archy: ratify or redirect Phase-0 plan; Rex: answer 5 prerequisites (Railway service, ANTHROPIC_API_KEY, spend cap, wakeble agent list, directory map) | Archy review + Rex prerequisites |
| **Telegram runner hardening (wake auth + retry + trust-all + flood)** (Archy commission 2026-07-07 eve) | **FIXES 1–4 BUILT + TESTED 2026-07-07 eve** (Fable) — branch `runner-hardening` → **PR #2**, 38/38 offline (14 new). Fix 1: wake env built from whitelist, ambient ANTHROPIC_API_KEY stripped (the 401), credential from runner/.env only, fail-loud w/ setup-token hint. Fix 2: retry-not-burn (3 att. exp. backoff, dead-letter, pre-dispatch attempt record = no double-spend across crash; is_error-on-exit-0 now fails). Fix 3: startup trust-all (merge-only ~/.claude.json). Fix 4: 🔴 baseline seeding. Handoff `output\specs\2026-07-07-runner-hardening-handoff.md`; card `39606d59…815f` → Blocked / 🔴 Rex (= BLOCKED-ON-REX push) | **Rex: `claude setup-token` → runner\.env**; Archy gate PR #2 → Rex merge → restart runner → live wake-proof (🟢 WAKE COMPLETE) | 🟠 BLOCKED-ON-REX: the token only Rex can mint — final DoD item |
| **Telegram two-way (P6/P7) — notification contract + reply-to-named-agent** (Archy commission 2026-07-07, Rex-ratified, 🔴 TODAY) | **BUILT + SELF-TESTED 2026-07-07** (Fable 5, unattended per §4 GREEN) — repo `Datavation/telegram-cockpit` branch `v5-two-way` → **PR #1** (main untouched = no deploy). P6: notify.py, only DECISION/DONE short-form pushes, idempotent + rollout-seeded. P7: routing.py reply→card writeback + addressed `[DISPATCH → agent]` envelope + runner\local_runner.py wakes the NAMED agent on the PC (identity/registry/cost gates, at-most-once). Folds in V0-hardening card (trust-bake · V5 · redaction) + hook write-lock defect. 24/24 offline; hook 8-payload verified. Handoff `output\specs\2026-07-07-telegram-two-way-handoff.md`; build card `39606d59…` In review / 🔴 Rex (= the §0 DONE push) | **Archy: gate-review PR #1** → Rex: enable Notion insert-comments, merge (=deploy), start runner, §5 live acceptance, rotate bot token | Archy gate + Rex promotion — control gate. Live E2E unprovable until deployed |
| **Telegram command interface (Epic 1 · Reachability)** | **SCAFFOLD STAGED** 2026-07-02 — `Builds\telegram-interface-v1\` (long-poll bot, allowlist-of-one, Whisper transcription, `claude -p` under fleet-guardrails, board 🔴 Rex exception push + reply-writeback). Offline tests GREEN (allowlist + dispatch cost-cap), py_compile clean. Board card In review. Pre-check resolved: headless = ANTHROPIC_API_KEY (metered, not Max sub); host = Railway-first | Archy review → Rex provisions (bot token, TG id, API key + workspace cap, Notion/OpenAI tokens, Railway service) + promotes; then V0–V8 (V0 = validate `claude -p` headless on Railway) | Rex provisioning + promotion — control gate. V0–V8 unprovable until mounted |
| **Holly full capability (WhatsApp + email + Notion capture integrity)** | **PROMOTED LIVE** 2026-07-02; Conversation Log DB created (`7a9ed2524f164b7aa8d74ddcaceacdba`) + `NOTION_CONVO_DB` set in Railway 2026-07-02 — P1 continuity live | Acceptance tests | — |
| **Holly email channel — Phase A (turn-on)** | **IN PROGRESS 2026-07-04** — Austen provisioning. Key correction: only mailbox is `info@rexhomeservices.co.uk`; holly@/austen@ are aliases → login/app-pw is on **info@**, `HOLLY_EMAIL_ADDRESS=info@…`. Runbook `output\specs\2026-07-04-holly-email-phase-a-turnon.md`. Railway svc renamed `web`→`holly-webhook`. IMAP ON (org). Vars added, deploying + testing from Datavation | Austen: finish acceptance test (Datavation→holly@ → reply + Notes/Log rows) | Cody gated out of Google + Railway |
| **Holly email channel — Phase B (shared-inbox hardening)** | **STAGED 2026-07-04** — `Builds\Holly-email-phase-b-v1\` (server.py + BUILD-HANDOFF + test). **4 changes:** reply-as holly@ (`HOLLY_EMAIL_FROM`), engage only mail To/Cc holly@ (`HOLLY_INBOUND_ADDRESS`), peek+Message-ID dedup (never marks shared inbox \Seen; twin of wamid cache), **HTML-only body fallback** (stdlib html.parser — fixes silent drop of HTML-only mail, found via live-log dig). py_compile clean, **17/17** offline suite pass. R-A reply hard-lock untouched (tightened, not widened) | Archy: review (one flag — record-on-detection dedup) → Rex promote (copy live + commit + set `HOLLY_EMAIL_FROM`) | Archy review + Rex promotion — control gate |
| **Holly webhook idempotency fix (duplicate reply bug)** | **LIVE** 2026-07-02 — commit fa90734, Railway deployed; single-reply acceptance test passed; Conversation Log DB connected | — | — |
| **Agent Output Record (Fleet Command Funnel, build 1 of N)** | **D1 LIVE** (2 board fields, gold-rail verified) · **D2 hook + D3 briefing STAGED** `Builds\fleet-output-record-v1\` + `Builds\standup-routine-v1\` 2026-06-30 | Archy review D2/D3 → **Rex promotes the hook** (Holly first); optional live scratch-row Part A test | Native-Windows hook exec unverified (C3/V8) → cloud-Routine seats first; local best-effort + Archy sweep |
| Decision Board → PMO (Build 1 of 4) | COMPLETE 2026-06-28 — board row *In review* | Archy ratify → Rex sign-off | — |
| Board Build 2 — Autonomy & Safety guardrails | COMPLETE 2026-06-28 — staged `Builds\fleet-guardrails-v1\`, board *In review* | Archy ratify → Austen promote+verify (V1–V8) → Rex account caps | Rex account-level spend/rate caps + cloud/WSL2 venue + managed-settings plan question |
| Board Build 3 — Stand-up Routine | LOCAL PILOT LIVE 2026-06-29 (`fleet-standup` 06:00/18:00); board → In review | Austen: "Run now" to pre-approve Notion tool; paste staged prompt into claude.ai/code for true PC-off (AC1); confirm Routines availability + cost | Cloud PC-off run needs the web Routines feature (not callable from here) |
| Board Build 4 — HTML command-centre | COMPLETE 2026-06-29 — staged `Builds\fleet-dashboard-v1\`; board → In review | Austen: Notion token + schedule sync + host static (GitHub Pages free) | — |
| **Agent Orchestration Layer (Builds 1–4)** | **ALL COMPLETE 2026-06-29** — all in Archy review | Archy ratify → Rex confirm; Austen: promote guardrails, host dashboard | — |
| Cody rebuild | COMPLETE — Archy ratified 2026-06-24 | — | — |
| Holly rebuild v2 | LIVE — deployed + verified (/health green) 2026-06-25; staging folder now vestigial | — | — |
| Agent-to-agent dispatch channel | Parked | Belongs with Holly's Dispatcher rebuild | Deliberately deferred 2026-06-24 |
| WhatsApp → Notion task capture | INTAKE — brief received 2026-06-27, held | Phase 0 design on hold until Archy clears | Archy gate + base-version decision (live vs Holly-v2) + Notion token prerequisite |
| Railway upgrade | DONE 2026-06-27 | Hobby plan active | — |
| Railway spend limit | DONE 2026-06-30 — Rex-approved limits set in Railway UI: Compute hard $15 / alert $10, Agent hard $15 / alert $5; board item closed | — | Cody gated out of Railway (no token) — limits applied by Austen/Rex |
| Railway GDPR migration | Open | US-West → EU-West | — |
| Anthropic API key rotation | Open risk | Rotate (exposed in debug screenshots 2026-06-03) | Before any production widening |
| Always-on hosting decision | Open | Bundle with library platform ruling | OneDrive sync ≠ execution |

### Holly rebuild v2 — what was built
- `CLAUDE.md` — v2 identity, Court of Rex framing, Archy/Cody accountability, memory v0.2
- `memory/MEMORY.md` — v0.2 format, 23 MEM entries migrated from flat prior version
- `memory/memory_log.md` — seeded with migration entry
- `webhook/server.py` — split-brain fix: context loading from GitHub replaces hardcoded HOLLY_BASE; fallback to baked-in baseline; stale-context WhatsApp notification; test scheduler slots removed; briefing time corrected to 06:30
- `webhook/requirements.txt`, `webhook/Procfile` — unchanged from live (copied)
- `webhook/.env.example` — GITHUB_TOKEN added
- `webhook/context/memory.md`, `people.md`, `personal.md`, `rex.md`, `businesses.md` — baseline snapshots; fetched live from GitHub at Railway startup

### Promotion checklist (human action after Archy ratification)
1. Copy `Builds\Holly-rebuild-v2\CLAUDE.md` → `Agents\Holly\CLAUDE.md`
2. Copy `Builds\Holly-rebuild-v2\memory\*` → `Agents\Holly\memory\*`
3. Copy `Builds\Holly-rebuild-v2\webhook\server.py` → `Agents\Holly\webhook\server.py`
4. Copy `Builds\Holly-rebuild-v2\webhook\.env.example` → `Agents\Holly\webhook\.env.example`
5. Create `Agents\Holly\webhook\context\` and copy all five context files into it
6. Commit all `webhook/` changes to `Datavation/holly-webhook` GitHub repo
7. Add `GITHUB_TOKEN` to Railway environment (fine-grained PAT, Contents read-only, holly-webhook repo only)
8. Restart Railway service; verify logs show `[context] loaded from GitHub:` × 5
9. `GET /health` → `context_stale: []`

-----

### Holly Channels v0.1 (Rex overnight commission 2026-07-18) — RULED 07-19; W DONE (staged), E blocked on cutover, V built (demo = Rex)
`Builds\Seats\Holly\holly-channels-v1\DESIGN.md` + board card 3a206d59-4638-81d6-827f-f3b88101c8ee.
One-brain-thin-mouths (WhatsApp/Email ride the runner pipe; Voice = honest second brain).
All 4 rulings landed 07-19; three parallel TDS sessions, standing session = coordinator.
**TDS-E session (07-19): build complete, NOT DONE** — allowlist v2 (exact + `@domain`,
fail-closed; adapter 59/59), poller headers-first (Gmail `[OVERQUOTA]` lesson), envelope
`Reply-to:`, seat snippet + runbooks staged. E1 smoke + live dedup BLOCKED: info@ IMAP
bandwidth exhausted by the live Railway email leg — needs Rex's cutover (unset
`HOLLY_EMAIL_ADDRESS` → restart → ≤24 h throttle clear) + his test mail. Evidence:
`email_channel\BUILD-HANDOFF-E-v0_1.md`; completion report in `output\specs\`.
**TDS-W session (07-19): DONE — STAGED, nothing deployed.** Relay mode behind
`HOLLY_RELAY_MODE` in a `whatsapp_relay\staging\` repo clone (server.py +36 lines;
off = byte-inert, proven). Return leg = Option A card-polling (15 s watcher, pending
store on the Railway volume, 30-min stuck ⚠️, dead-letter relay, 24 h loud give-up).
30/30 offline green incl. envelope byte-compat vs the REAL `local_runner.parse_envelope`.
Findings: live server has NO voice-note transcription (TDS assumed one — text-only
preserved, follow-up brief); Railway `NOTION_TOKEN` can't see the Board → new
`HOLLY_BOARD_NOTION_TOKEN` env. `seat-patch-snippet.md` + `PROMOTION-RUNBOOK.md` ready
for the coordinator's merged promote. Handoff + completion report in `output\specs\`
(2026-07-19). Card 3a206d59-…-c685e1733307 → In review / ⚪ None.
**TDS-V session (07-19): build complete + offline-proven, NOT DONE (live demo = Rex's hands).**
Built ON the POC per v0.2 addendum: engine gained `get_diary` (shells seat calendar_adapter,
live smoke 19 real events) + `log_followup` (poller-shape board card + `[VOICE from Rex]`
envelope, byte-compat vs REAL `parse_envelope`) + no-guess rule verbatim — baked into
`rebase_holly.py` derivation (LESSON: direct `agents\holly\` edits are clobbered by the
gold-rail regeneration; deltas must live in the derivation). New suite 25/25; selftest 21/21
+ phase2 21/21 re-green post-edit. No Railway/server.py change (W-merge note VOID). Archy
split-brain proposal card filed (3a206d59-…-fcd53b6a5d3c, To=Archy). V card → In review /
🔴 Rex: demo per `voice\HANDOFF-REX.md` (adapter restart + tunnel paste + done-test).
Second leg (Rex ruling same day): `voice\web\holly.html` — ElevenLabs widget page,
browser-verified (secure ctx, widget initializes; agent id → localStorage only). Fable
artifact hosting = structurally impossible (CSP). Always-on staged in engine `tunnel\`
(named tunnel + Fleet-VoiceAdapter/VoiceTunnel tasks; datavation.ai-DNS-on-Cloudflare
gate flagged). After demo: Rex names Vercel property → Cody deploys page for phone.

### Holly minimal build (brief v0.1, 2026-07-17) — WHOLE BRIEF COMPLETE 2026-07-18
Phase 2 passed same day: Fleet-TelegramRunner as scheduled task, phone-proven unattended;
Fleet-Cockpit stays Disabled (resource pig). Archy lessons-learned card filed (RELAY).
- 1a done-test PASSED 2026-07-18: phone → real calendar event → one-sentence Holly reply
  ($0.39 wake). Credential = scoped SA key (org-policy override → key → re-locked);
  promote.py run by Rex; runner phone-reply leg live in the manual-run runner.
- 1b email BUILT + staged 2026-07-18: `adapter\email_adapter.py` (allowlist in
  C:\AgentEngine\secrets\, structural, closed-by-default; policy checks stubbed TODO),
  36/36 offline; `promote_1b.py` ready. BLOCKED on Rex: run promote_1b.py + Gmail
  app-password into holly-email.env.
- Ack suppression (cockpit) staged, 84/84 — live only on next Railway redeploy (Rex).
- Cockpit repo: 3 clean commits on env-relocation, NOT pushed (push/deploy = Rex's call).
- Pollers are manual-run for testing only (always-on killed the PC) — durable host is
  the open Phase 2 question, bundle with Railway redeploy decision.

-----

### Fleet Dashboard on Vercel + Notion change-log relay (Archy TDS v0_1, 2026-07-18) — STAGED + PUSHED 2026-07-18
Built to `Oz Files\Design\Fleet-Dashboard-Vercel-TDS-v0_1.md`. Staged at
`Builds\Dashboardsleet-dashboard-vercel\`, repo `Datavation/fleet-dashboard` (private, main).
- ✎ → `/api/propose` → Canon Change Log DB (bfa2bfe1…, only Notion write path, token in
  Vercel env only); file:// download fallback kept. Generators edited + regenerated
  (`_render_canon_html.py`, `_render_home.py`). Local preview verified end-to-end.
- BLOCKED on Rex (3 min): Vercel import (vercel.com/new, Chrome not connected in the
  unattended run) → NOTION_TOKEN env var → Deployment Protection (Vercel Auth).
- PARKED for Archy ruling: /api/board GET/PUT conflicts with the only-write-path gate;
  Kanban keeps Save/Load. Also: sweep source change to Status=open rows is Archy's tooling.
- Handoff: `output\specs6-07-18-fleet-dashboard-vercel-handoff.md`.

*Kept current per the rebuild rules. Anything architecture-touching is proposed in
`memory_log.md`, not decided here.*

-----

### Console real data — live Decision Board in Fleet Command (Archy commission, 2026-07-19)
Rex signed off console UX → gate lifted. Built same morning, in parallel with the
vercel-fixes session (venue.js/engine.js — scopes interlocked, no conflicts).
- `/api/board-data` (Vercel serverless): read-only board feed, GET-only, one Notion
  route (data_sources query), token server-side, full pagination, loud-fail everywhere.
  Canonical DS id 2f12180d… (commission's 095e6b08… is a Notion alias — verified).
- `loadBoard()` venue-routed: hosted→/api/board-data, cockpit→/api/board, file→stub.
  adaptBoard() keeps the STUB_BOARD contract; STATUS_TO_COLUMN total map (no card can
  vanish); FIXTURES stay for chats/profiles/feed/roster-presence.
- Tests: pc-cockpit 87 passed (8 new parity/gate tests; fixed pre-existing CRLF drift
  in telegram-runner ops copies); dashboard repo 7 passed. Live read: 171 cards.
- Handoff: `output\specs\2026-07-19-console-real-data-handoff.md`. Board card → Archy
  (Review) at deploy.

-----

### Console Vercel fixes — key-gate, Fleet-engine panel, stub cosmetic (Archy commission, 2026-07-19) — LIVE
Built same morning as console-real-data (scopes interlocked via venue.js; one combined
deploy cadc6a2 + cad9867). All 3 fixes live-verified; completion report DONE (9/9 PASS).
- Fix 1: hosted venue (https non-local) boots keyless — Vercel deployment protection is
  the gate; cockpit venue keeps the lock. ⚠ depends on the 🔴 Rex card: production alias
  currently serves WITHOUT login (protection not covering production).
- Fix 2: SYSTEM · FLEET ENGINE panel on the Dispatch screen, separate from Holly's card:
  status/heartbeat/interval/rate-cap/feed-age, \Fleet\ task states, live wakes, Refresh,
  confirmed Stop/Start → /api/engine (closed {stop,start} allowlist) → Notion command
  block → `agent-dispatcher-v1\engine_relay.py` → dispatcher KILL file (at-most-once,
  Telegram-alerted). One status source: health.js feeds pill + strip + panel.
  AWAITING REX: Notion page + ENGINE_STATUS_PAGE_ID (Vercel + agent-dispatcher.env) +
  `install_relay_task.ps1` — until then the panel/pill show neutral "ENGINE FEED NOT WIRED".
- Fix 3: stub/no-feed = neutral ◌ state; red reserved for real outages (unreachable /
  empty / stale feed). Old ⛔-over-stub gone from the live site.
- Tests: relay 15/15 · repo gates 7/7 · pc-cockpit 87/87 · dispatcher 21/21 + 12/12.
  sync_site.py hardened vs OneDrive read-only rmtree failures.
- Handoff + completion report: `output\specs\2026-07-19-console-vercel-fixes-handoff.md`.
  Board card → Archy (Review), filed + verified.
- ADDENDUM same day (Rex-approved): console auth gate (api/_auth.js, CONSOLE_ACCESS_KEY,
  fail-closed; lock screen back on hosted — Hobby tier can't protect the production alias)
  + live-derived plumbing (real roster/workload/resourcing, live Pulse KPIs, activity feed,
  🔴-Rex alerts, real linked tasks; no fake chats/profiles under real seats). Deployed
  6c65159→0d614a0; public URL verified CLOSED (503) until Rex sets CONSOLE_ACCESS_KEY.
  🔴 card retitled with the 3-min key steps; Archy review comment added.
- PM SESSION 2: CONSOLE_ACCESS_KEY confirmed working by Rex (card closed). Engine feed
  PC-side ACTIVATED: status page = board card 3a206d59-4638-817c…, env set, Fleet-EngineRelay
  registered+running (Rex-requested), pushes verified. Routines screen live from engine tasks
  (fixture fallback). Mobile pass (fc-* classes + ≤700px media queries) verified 375/1280px.
  Deployed 61d24ac. 🔴 open: Rex adds ENGINE_STATUS_PAGE_ID Vercel env (1 min) → panel+light live.
  NOT built (no source/architecture): agent chats, Projects, A2A, cost/time telemetry.
- PM SESSION 3 (chat): TDS-console-chat-v0_1 written + BUILT same session (new regime:
  Archy reviews the doc; card 3a206d59-4638-8151). /api/chat + relay chat handling +
  chatlive.js pane deployed 51feec4; relay restarted; runner untouched. Tests 24+10+88.
  E2E: relay→marker→Holly wake in flight at session close; Rex's phone test = final leg.
