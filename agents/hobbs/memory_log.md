# memory_log.md — Hobbs (Consilium) — cloud seat

> **HELD — see MEMORY.md.** The append-only log records the same personal/sensitive threads as the
> canonical memory, so it is held out of Git on the same basis, pending Archy/Rex's ruling on the
> home for personal-class seat memory. The authoritative log stays on private storage (OneDrive).
