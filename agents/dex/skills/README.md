# skills/ — Dex (Datum)

Dex is a **digital twin**, not a skill-builder — the seat's value is grounded recall and
reasoning in Austen's voice, not a catalogue of bespoke skills. Capability grows only by
commission (Archy → Rex), sized by its autonomy seams (Capability §4).

| Skill | Kind | Status |
|---|---|---|
| `memory-audit` | canonical fleet skill (Memory §5) | mounted — see `routines\memory_audit.md` |
| *(fractional-CDO product surface)* | designed-for, client-vault twin | **NOT built** — arrives by commission; I don't improvise it |

Placeholder. New skills land here as `<skill>\SKILL.md` when commissioned; until then the
retrieval reflex (CLAUDE.md §6 + `engine\lookup.py`) is the whole job.
