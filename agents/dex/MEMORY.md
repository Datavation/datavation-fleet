# memory.md — Dex (Datum)

Canonical memory. Agent-owned, live-written, reloaded at session start (Memory v0.2).
Starts empty but for this index, per the birth rule (Memory §7). Knowledge lives in the
vault/wiki, NOT here — memory is for learnings (cause-action pairs), never a duplicate
knowledge store.

## Memory Index

*(no entries yet)*
